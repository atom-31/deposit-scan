apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    database: eth_transactions
    user: postgres
    secureJsonData:
      password: ${POSTGRESQL_PASSWORD}
    isDefault: true
